import React,{useState} from 'react'; import {Radio,ChevronRight} from 'lucide-react'; import {Tabs} from '../../components/ui/Tabs.jsx'; import {SOSCard} from './SOSCard.jsx'; import {sosRequests} from '../../data/mockSOSRequests.js';
export function SOSFeed({onDispatch}){const [tab,setTab]=useState('Critical');return <div className="panel"><div className="panel-title"><span><Radio size={17}/> LIVE CITIZEN DISTRESS SOS FEED</span><span className="live-dot">● LIVE</span></div><Tabs items={['Critical','In-Progress','Resolved']} value={tab} onChange={setTab}/><div className="sos-list">{(tab==='Critical'?sosRequests:tab==='In-Progress'?sosRequests.slice(1):[]).map(x=><SOSCard key={x.id} item={x} onDispatch={()=>onDispatch(x)}/>)}</div><button className="view-all">View All Incidents <ChevronRight size={15}/></button></div>}
