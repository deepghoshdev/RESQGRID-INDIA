import React from 'react'; import {Radio,ArrowRight,Repeat2,MapPin} from 'lucide-react'; import {Button} from '../../components/ui/Button.jsx'; import {Badge} from '../../components/ui/Badge.jsx';
export function SOSCard({item,onDispatch}){return <article className="sos-card"><div className="sos-icon"><Radio size={22}/></div><div className="sos-main"><div className="row between"><span className="incident">Incident {item.id}</span><Badge tone="red">CRITICAL</Badge><span className="muted">{item.age}</span></div><h3>{item.title}</h3><p>{item.place}</p><div className="meta"><span>Type: <b>{item.type}</b></span><span>Water Level: <b className="red-text">{item.level}</b></span></div><div className="meta"><span><MapPin size={12}/> {item.lat}</span><span>{item.lng}</span></div><div className="card-actions"><Button variant="primary" onClick={onDispatch}>Accept & Dispatch Team <ArrowRight size={15}/></Button><Button><Repeat2 size={15}/> Transfer to SDRF</Button></div></div></article>}
