import React,{useState} from 'react'; import {Modal} from '../../components/ui/Modal.jsx'; import {Button} from '../../components/ui/Button.jsx'; export function BroadcastAlertModal({open,onClose}){const [message,setMessage]=useState('');return <Modal open={open} onClose={onClose} title="National / Statewide Emergency Alert"><p className="modal-copy">This demo composes a high-priority alert. No external broadcast is sent.</p><textarea className="textarea" value={message} onChange={e=>setMessage(e.target.value)} placeholder="Enter alert message..."/><Button variant="danger" onClick={onClose}>Broadcast Demo Alert</Button></Modal>}
